<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->

        <item name="android:actionMenuTextColor">@android:color/white</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionModeStyle">@style/MyTheme.ActionMode</item>
        <item name="android:actionBarTabStyle">@style/ActionBarTabStyle.Pmp</item>


    </style>

    <style name="MyTheme.ActionMode" parent="@android:style/Theme.Holo.Light">
        <item name="android:height">?android:attr/actionBarSize</item>
        <item name="android:background">@color/light_bronze</item>
        <item name="android:backgroundSplit">@color/light_bronze</item>
        <item name="android:actionModeBackground">@color/light_bronze</item>

    </style>

    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/dark_blue</item>
        <item name="android:backgroundStacked">@color/light_blue</item>
        <item name="android:titleTextStyle">@style/MyTheme.ActionBar.TitleTextStyle</item>
    </style>

    <style name="MyTheme.ActionBar.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="ActionBarTabStyle.Pmp" parent="@android:style/Widget.Holo.Light.ActionBar.TabView">
        <item name="android:background">@drawable/tab_indicator_ab_pmp</item>
    </style>

    <!--</style>-->

    </resources>
